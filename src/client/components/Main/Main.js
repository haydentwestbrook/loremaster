import React from 'react';

const Main = () => {
  const click = () => {};

  return (
    <div className="main">
      <h1>React App</h1>
      <button onClick={click}>Click</button>
    </div>
  );
};

export default Main;
